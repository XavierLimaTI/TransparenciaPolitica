# Environment template for local development# Copy to .env or export variables in your shell# Portal da Transparência API key (optional if using proxy)PORTAL_KEY=# Proxy base URL (used in CI and local testing)PROXY_BASE=http://127.0.0.1:3001# Webhooks receiver URLWEBHOOKS_BASE=http://127.0.0.1:3002# ADMIN token for admin endpoints (generate via scripts/generate-admin-token.ps1 or set in CI)ADMIN_TOKEN=
# WEBHOOK secret to verify incoming webhooks (HMAC sha256)
WEBHOOK_SECRET=

# Sync robustness
SYNC_FAILURE_THRESHOLD=5
SYNC_COOLDOWN_MS=300000
